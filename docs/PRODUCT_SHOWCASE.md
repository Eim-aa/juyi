# 句译产品展示素材

## 当前交付

- `media/selection-demo.mp4`：8 秒、1280 × 896、无音轨的真实 PDF 标题翻译实录。
- `media/selection-demo.gif`：同片段的 12 fps 首页预览；`media/selection-demo-poster.jpg` 为实录帧。
- 实录来源：用户 2026-09-25 18:24:51 的整屏录像，取第 1–9 秒。原始视频保留不动；裁切区域为 1600 × 1120，左上角 (900, 370)，移除桌面、Dock、菜单栏和语音悬浮工具条。没有加速、替换译文或拼接伪造操作。视频来自 build 10，不冒充 build 11 修复后的端到端验收；单次耗时不是速度承诺。

- `media/overview.svg`：1200 × 700 的可编辑操作示意图；明确标注“非实机录屏”，未捏造译文、性能数字或兼容性结果。
- `media/overview.png`：由 SVG 渲染的 2400 × 1400 展示图，供 README 使用以保持文字排版一致；已检查完整画布与三张步骤卡，无裁切。
- `media/home-ready.jpg`：已安装本地候选的真实首页截图。
- `media/settings-local-cloud.jpg`：同一候选的真实翻译方式展开截图。
- 中英文 README：价值介绍、获取入口、安装前提、首次翻译步骤、真实界面和支持范围。

截图采集于 2026-09-25，本地版本 0.4.0 build 10。截图来自安装到 Applications 的应用，未修改界面像素，不含用户文稿或密钥。该本地候选尚未公开发布。用户已在本次会话确认更新后选区翻译正常；该口头验收不是录屏证据。应用仍为 ad-hoc 本地签名，不宣称已完成 Developer ID、公证或 App Store 上架。

2026-09-25 通过 GitHub CLI 只读检查，仓库 Release 列表为空。README 因此提供“查看发布状态”与源码试用入口，不提供伪装成安装包的直接下载按钮。发布前需再次检查并更新状态。

## 后续素材：修复后长段落实录

目标约 15–20 秒，只用无敏感信息的示例文稿，不在私人网页、聊天、真实论文批注或账户页面录制。

1. 0–4 秒：展示文本编辑中的示例句 `Good tools should feel effortless.`，鼠标真实选中英文。
2. 4–7 秒：由本人实际连按两次 Option；可以添加操作字幕，但不伪造系统输入或结果。
3. 7–13 秒：保留真实等待过程并展示实际生成的中文浮窗；不替换译文，不虚构耗时。
4. 13–18 秒：回到阅读画面；结尾标注“macOS 15+ · 开发者预览 · 安装前提见 README”。

上面的文本编辑脚本仍可作为后续素材；当前首页已有用户提供的 PDF 标题实录。build 11 的断词长段落实录仍待补录，不能用 build 10 素材替代。全局快捷键需本人执行；当前 Computer Use 不能代替真实全局按键验收。旧 `demo.gif` 为绘制演示，已从首页移下，原文件保留以免破坏历史引用。

## 发布前检查

- GitHub 展示：确认图片能显示、移动端可读、相对路径正确、没有私人信息；README 与拟发布版本一致。
- 公众安装包：正式签名、公证、分发包、干净账户安装验收完成后，再把下载入口指向具体 Release 及校验值。
- App Store：先独立评估当前辅助功能、Hammerspoon 和后台组件架构的适配；不要把 Developer ID 公证当成商店审核通过。评估并确认当时的提交要求后，另制对应商店截图，不直接复用 README 长图，不提前使用已上架徽章。
- 可从现有素材延展三个商店叙事方向：就地阅读、Apple 本地处理、翻译方式与隐私。它们目前仅是素材方向，不是可提交的完整商店资产。

## 素材维护

保留现有已选定图标。示意图是源码 SVG，可直接编辑文字与布局；真实截图需要在相应版本重新采集，不用绘图覆盖旧截图文字。界面变化时同时更新中英文 README。不得宣传任意 App、OCR、任意云服务或固定翻译速度。
